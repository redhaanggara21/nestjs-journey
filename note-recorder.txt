docker build -t nest-journey-2 . build the images base on dockerfile
docker run -p80:3000 nest-journey-2 running the image building

docker run -it image_name sh
docker image inspect

But production stage better not have COPY . .